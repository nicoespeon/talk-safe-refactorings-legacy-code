Now we can inject a different `GrandCentral` in tests 🎉

## If we use TS, there's a problem

It still expects the problematic `Clients.GrandCentral`

We need to "Extract Interface"

In JS, it's implicit (duck typing)
